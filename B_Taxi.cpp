#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int grp[n];
    for (int i = 0; i < n; i++)
    {
        cin >> grp[i];
    }
    
    // int total = accumulate(grp, grp+n, 0);
    // if(total%4 != 0) cout << (total/4)+1;
    // else cout << total/4;
    return 0;
}